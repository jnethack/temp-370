--- ../../nethack/src/engrave.c	2023-02-16 06:52:56.000000000 +0900
+++ ./engrave.c	2026-06-07 02:06:47.915763300 +0900
@@ -89,7 +94,11 @@
 int cnt;
 unsigned seed; /* for semi-controlled randomization */
 {
+#if 0 /*JP*/
     char *s;
+#else
+    unsigned char *s;
+#endif
     int i, j, nxt, use_rubout, lth = (int) strlen(engr);
 
     if (lth && cnt > 0) {
@@ -1173,6 +1208,10 @@
             if (!(*sp == ' '))
                 maxelen--;
         if (!maxelen && *sp) {
+#if 1 /*JP*//*漢字の1バイト目だけが残らないように*/
+            if(is_kanji2(ebuf, sp - ebuf))
+                --sp;
+#endif
             *sp = '\0';
             if (multi)
                 nomovemsg = "You cannot write any more.";
